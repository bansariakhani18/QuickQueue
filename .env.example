# Local Docker PostgreSQL configuration only. Do not use these values in production.
POSTGRES_DB=quickqueue
POSTGRES_USER=quickqueue
POSTGRES_PASSWORD=quickqueue_dev_only
POSTGRES_PORT=5432

# Reserved for the forthcoming Prisma phase. No schema or Prisma client exists yet.
DATABASE_URL=postgresql://quickqueue:quickqueue_dev_only@localhost:5432/quickqueue?schema=public

# Local application URLs
FRONTEND_ORIGIN=http://localhost:5173
BACKEND_PORT=3000
VITE_API_BASE_URL=http://localhost:3000
